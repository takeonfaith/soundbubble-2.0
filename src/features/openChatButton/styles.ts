import styled from 'styled-components';
import { Button } from '../../shared/components/button';

export const OpenChatButtonStyled = styled(Button)`
    min-width: 40px;
    padding: 0 8px;
    height: 40px;
`;
